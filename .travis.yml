language: php

php:
  - 5.6
  - 7.0
  - 7.1
  - hhvm
  - nightly

before_script:
  - composer self-update
  - composer install --no-interaction

script:
  - vendor/bin/phpunit

matrix:
  fast_finish: true
  allow_failures:
  - php: 5.6
  - php: hhvm
  - php: nightly

